---
title: カスタムAPIルート
---

```typescript showLineNumbers
import { Mastra } from "@mastra/cli/cli/cli/core";
import { registerApiRoute } from "@mastra/cli/cli/cli/core/server";

export const @@mastra/cli/cli/cli = new Mastra({
  server: {
    apiRoutes: [
      registerApiRoute("/my-custom-route", {
        method: "GET",
        handler: async (c) => {
          const @@mastra/cli/cli/cli = c.get("@actus-ag/@@mastra/cli/cli/cli");
          const agents = await @@mastra/cli/cli/cli.getAgent("my-agent");
          return c.json({ message: "Hello, world!" });
        },
      }),
    ],
  },
});
```
